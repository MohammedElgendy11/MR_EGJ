using UnityEngine;

public class Mashrabya_Tags : MonoBehaviour
{


    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        transform.Rotate(0f, 180f, 0f);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
